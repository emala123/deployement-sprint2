const Erreur = require("../models/http-erreur");

const Employeur = require("../models/employeur");
const Stage = require("../models/stage");
const { default: mongoose } = require("mongoose");

const inscriptionEmployeur = async (requete, reponse, next) => {
    // connecter l'utilisateur - regarder dans le backend du prof de la session passer pour le dernier projet mis
}